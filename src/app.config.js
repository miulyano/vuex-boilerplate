// App-specific config

// Using CommonJS instead of ES2015+ export, because we also need to
// provide this object to Webpack in vue.config.js.
module.exports = {
  title: 'Vuex Boilerplate',
  description: 'Quick start of your application with the configured data distribution system and the test server on Node and MongoDB'
};
